global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  - job_name: litellm
    static_configs:
      - targets: ["litellm:9090"]
    metrics_path: /metrics
